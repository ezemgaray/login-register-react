
const initialState = {
    test: 'test combine reducers',
    message: 'May can be an initial global state'
}

const todos = (state = initialState, action) => {
  return state
}

export default todos